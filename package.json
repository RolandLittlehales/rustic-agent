{
  "name": "rustic-agent",
  "version": "0.1.0",
  "description": "Rustic Agent - LLM Development Agent built with Rust + Tauri",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "tauri build",
    "tauri": "tauri",
    "icons:generate": "node scripts/generate-icons.js",
    "icons:install-deps": "sudo apt-get update && sudo apt-get install -y imagemagick"
  },
  "keywords": [
    "tauri",
    "rust",
    "llm",
    "development",
    "agent"
  ],
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "sharp": "^0.33.0",
    "to-ico": "^1.1.5"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0"
  }
}
